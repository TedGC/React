function searchBar({ onSubmit }) {
    const handleClick = () => {
        onSubmit('cars')
    }


    return <div>
        <button onClick={handleClick}></button>
    </div>dfasdfsa
}

export default searchBar; 